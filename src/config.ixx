export module config;

export namespace cfg
{
	inline constexpr int initialWidth = 800;
	inline constexpr int initialHeight = 800;

	inline constexpr int maxTrajectoryLength = 10000;
	inline constexpr float initialCubeSize = 1.0f;
	inline constexpr float initialCubeDensity = 1.0f;
}